#include "main.h"

/**
 * main - tests if number if positive or negative
 * and the number being tested here is 0
 * Return: o for success
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
