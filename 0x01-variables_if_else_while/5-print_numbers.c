#include <stdlib.h>

#include <time.h>

#include<stdio.h>

/**
 * main - Entry point
 * Return: Always return 0 (Success)
 */

int main(void)
{
	int  numbr = 0;

	while (numbr <= 10)
	{	printf("%d", numbr);
		numbr;
	}

	putchar('\n');
	return (0);
}
